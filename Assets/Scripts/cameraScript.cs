using UnityEngine;

public class cameraScript : MonoBehaviour
{
    public int cameraID;
    public bool isCamera = true;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
